{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python backend/manage.py migrate && python backend/manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT --proxy-headers config.asgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
